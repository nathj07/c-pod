This file was installed on <%= Time.now %>

The machine name is <%= node[:fqdn] %> and it has kernel <%= node[:kernel][:release] %>

<% if node[:virtualization][:system] == 'vmware' %>
  It's running as a VMware virtual machine
<% else %>
  It's physical hardware
<% end %>

It also has <%= node[:memory][:total] %> memory and <%= node[:memory][:swap][:total] %> swap
